# DA AUTOLIGHT AI — Backend Python Dependencies
# Install with: pip install -r requirements.txt
# --------------------------------------------------------

# Web framework & ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Environment variable loader
python-dotenv==1.0.1

# Async HTTP client (for Telegram API calls)
httpx==0.27.0

# Data validation & settings management
pydantic==2.7.0

# Google Gemini 1.5 Flash (AI engine)
google-generativeai

# Supabase database client
supabase==2.5.0

# --------------------------------------------------------
# FUTURE (uncomment when moving to Phase 2+)
# --------------------------------------------------------
# redis==5.0.4                  # Persistent session storage
# langchain==0.2.0              # RAG pipeline for WhatsApp tone embeddings
# langchain-google-genai==1.0.6 # Gemini embeddings for LangChain
# chromadb==0.5.0               # Local vector DB for WhatsApp embeddings
